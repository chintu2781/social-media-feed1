# Social Media Feed

A real-time social media feed application built with React (frontend) and Node.js with WebSocket (backend).

## Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/social-media-feed.git
   cd social-media-feed
